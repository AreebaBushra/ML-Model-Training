# Streamlit dashboard dependencies.
# Install with: pip install -r requirements-streamlit.txt
# (backend/requirements.txt stays lean for the Render deployment)

streamlit>=1.32.0
plotly>=5.19.0
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.4.0
joblib>=1.3.0
pydantic>=2.6.0
requests>=2.31.0

# Only needed if you also want to run the FastAPI backend locally
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
